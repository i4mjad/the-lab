{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Host-neutral independent review output for all five artifact gates and adversarial code review. Fields that do not apply are nullable for cross-host structured-output compatibility.",
  "type": "object",
  "additionalProperties": false,
  "required": ["verdict", "summary", "findings", "next_steps"],
  "properties": {
    "verdict": {
      "type": "string",
      "enum": ["approve", "needs-attention"]
    },
    "summary": {
      "type": "string",
      "description": "Terse ship/no-ship assessment. Lead with the claim; no preamble, no recap."
    },
    "findings": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "id",
          "severity",
          "title",
          "body",
          "recommendation",
          "confidence",
          "quote",
          "file",
          "line_start",
          "line_end"
        ],
        "properties": {
          "id": {
            "type": "string",
            "description": "Short stable handle, e.g. X1, X2. The challenge round addresses findings by id."
          },
          "severity": {
            "type": "string",
            "enum": ["blocker", "major", "minor"]
          },
          "title": { "type": "string" },
          "body": { "type": "string" },
          "recommendation": { "type": "string" },
          "confidence": {
            "type": "number",
            "minimum": 0,
            "maximum": 1,
            "description": "0 to 1. Keep it honest — you will be asked to defend this finding."
          },
          "quote": {
            "type": ["string", "null"],
            "description": "Document gates: verbatim text from the artifact this finding is about. null when the finding is about content that is ABSENT, or in code-review mode."
          },
          "file": {
            "type": ["string", "null"],
            "description": "Code review: repo-relative path. null in document-gate mode."
          },
          "line_start": { "type": ["integer", "null"] },
          "line_end": { "type": ["integer", "null"] }
        }
      }
    },
    "next_steps": {
      "type": "array",
      "items": { "type": "string" }
    }
  }
}
